---
---

<section id="contacto" class="contact-section">
	<div class="container">
		<div class="contact-content">
			<div class="contact-header">
				<h2 class="section-title">
					Hablemos
				</h2>
				<p class="section-subtitle">
					¿Tienes un proyecto en mente? Estoy disponible para nuevas oportunidades.
				</p>
			</div>

			<div class="contact-methods">
				<!-- Email -->
				<a href="mailto:jhon.mendoza@ejemplo.com" class="contact-card">
					<div class="card-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<rect x="2" y="4" width="20" height="16" rx="2"/>
							<path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
						</svg>
					</div>
					<div class="card-content">
						<h3 class="card-title">Email</h3>
						<p class="card-text">jhon.mendoza@ejemplo.com</p>
					</div>
				</a>

				<!-- LinkedIn -->
				<a href="https://linkedin.com/in/jhonmendoza" target="_blank" rel="noopener noreferrer" class="contact-card">
					<div class="card-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/>
							<rect x="2" y="9" width="4" height="12"/>
							<circle cx="4" cy="4" r="2"/>
						</svg>
					</div>
					<div class="card-content">
						<h3 class="card-title">LinkedIn</h3>
						<p class="card-text">Conecta conmigo</p>
					</div>
				</a>

				<!-- GitHub -->
				<a href="https://github.com/jhonmendoza" target="_blank" rel="noopener noreferrer" class="contact-card">
					<div class="card-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"/>
							<path d="M9 18c-4.51 2-5-2-7-2"/>
						</svg>
					</div>
					<div class="card-content">
						<h3 class="card-title">GitHub</h3>
						<p class="card-text">Revisa mis proyectos</p>
					</div>
				</a>

				<!-- WhatsApp -->
				<a href="https://wa.me/573001234567" target="_blank" rel="noopener noreferrer" class="contact-card">
					<div class="card-icon">
						<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M3 21l1.65-3.8a9 9 0 1 1 3.4 2.9L3 21"/>
							<path d="M9 10a.5.5 0 0 0 1 0V9a.5.5 0 0 0-1 0v1a0 0 0 0 0 0 0"/>
							<path d="M14.5 9.5a.5.5 0 0 0-1 0v1a.5.5 0 0 0 1 0v-1"/>
						</svg>
					</div>
					<div class="card-content">
						<h3 class="card-title">WhatsApp</h3>
						<p class="card-text">Chatea conmigo</p>
					</div>
				</a>
			</div>

			<!-- Availability Badge -->
			<div class="availability">
				<span class="status-dot"></span>
				<span class="availability-text">Disponible para proyectos freelance</span>
			</div>
		</div>
	</div>
</section>

<style>
	.contact-section {
		padding: var(--space-20) 0;
		background: var(--white);
		position: relative;
	}

	.container {
		max-width: 900px;
		margin: 0 auto;
		padding: 0 clamp(1rem, 5vw, 3rem);
		width: 100%;
	}

	/* ===== CONTENT ===== */
	.contact-content {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
	}

	/* ===== HEADER ===== */
	.contact-header {
		margin-bottom: var(--space-12);
		max-width: 600px;
	}

	.section-title {
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 800;
		color: var(--text-primary);
		margin-bottom: 1rem;
		letter-spacing: -0.02em;
	}

	.section-subtitle {
		font-size: clamp(1.125rem, 2.5vw, 1.375rem);
		color: var(--text-secondary);
		line-height: 1.6;
	}

	/* ===== CONTACT METHODS ===== */
	.contact-methods {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 1.5rem;
		width: 100%;
		margin-bottom: var(--space-8);
	}

	.contact-card {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		padding: 2rem 1.5rem;
		background: var(--white);
		border: 2px solid var(--secondary-300);
		border-radius: 1rem;
		text-decoration: none;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		cursor: pointer;
	}

	.contact-card:hover {
		border-color: var(--primary-500);
		transform: translateY(-8px);
		box-shadow: 0 12px 40px rgba(59, 130, 246, 0.15);
	}

	.contact-card:hover .card-icon {
		background: var(--primary-500);
		color: var(--white);
		transform: scale(1.1);
	}

	.card-icon {
		width: 72px;
		height: 72px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 1rem;
		background: var(--secondary-100);
		color: var(--primary-500);
		transition: all 0.3s ease;
	}

	.card-content {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.card-title {
		font-size: 1.125rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0;
	}

	.card-text {
		font-size: 0.875rem;
		color: var(--text-secondary);
		margin: 0;
	}

	/* ===== AVAILABILITY ===== */
	.availability {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 1rem 2rem;
		background: var(--secondary-100);
		border-radius: 3rem;
		margin-top: 2rem;
	}

	.status-dot {
		width: 10px;
		height: 10px;
		border-radius: 50%;
		background: #10B981;
		box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.2);
		animation: pulse-dot 2s ease-in-out infinite;
	}

	@keyframes pulse-dot {
		0%, 100% {
			box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.2);
		}
		50% {
			box-shadow: 0 0 0 8px rgba(16, 185, 129, 0.1);
		}
	}

	.availability-text {
		font-size: 0.875rem;
		font-weight: 600;
		color: var(--text-primary);
	}

	/* ===== RESPONSIVE ===== */
	@media (min-width: 640px) {
		.contact-methods {
			grid-template-columns: repeat(2, 1fr);
		}
	}

	@media (min-width: 1024px) {
		.contact-methods {
			grid-template-columns: repeat(4, 1fr);
		}
	}

	@media (max-width: 640px) {
		.contact-methods {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.contact-card {
			padding: 1.5rem 1rem;
		}

		.card-icon {
			width: 64px;
			height: 64px;
		}

		.availability {
			padding: 0.875rem 1.5rem;
		}
	}

	/* ===== ANIMACIONES ===== */
	@media (prefers-reduced-motion: no-preference) {
		.contact-header {
			animation: fadeInUp 0.6s ease-out;
		}

		.contact-card {
			opacity: 0;
			animation: fadeInUp 0.6s ease-out forwards;
		}

		.contact-card:nth-child(1) { animation-delay: 0.1s; }
		.contact-card:nth-child(2) { animation-delay: 0.2s; }
		.contact-card:nth-child(3) { animation-delay: 0.3s; }
		.contact-card:nth-child(4) { animation-delay: 0.4s; }

		.availability {
			animation: fadeInUp 0.6s ease-out 0.5s backwards;
		}

		@keyframes fadeInUp {
			from {
				opacity: 0;
				transform: translateY(30px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	}
</style>
