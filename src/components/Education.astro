---
import estudiante from '../assets/Perfil-programador.png';
import { Image } from 'astro:assets';
---

<section id="educacion" class="education-section">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title">
				Formación <span class="text-gradient">Académica</span>
			</h2>
			<p class="section-subtitle">
				Un camino de aprendizaje y desarrollo profesional
			</p>
		</div>

		<div class="education-grid">
			<!-- Timeline de Educación -->
			<div class="education-timeline">
				<!-- SENA -->
				<div class="timeline-item">
					<div class="timeline-marker">
						<div class="marker-dot"></div>
						<div class="marker-line"></div>
					</div>
					<div class="timeline-content">
						<h3 class="degree-title">Tecnólogo en Análisis y Desarrollo de Software</h3>
						<div class="institution">Servicio Nacional de Aprendizaje (SENA)</div>
						<div class="field">Tecnología de la Información</div>
						<div class="education-date">Julio de 2024 - Octubre de 2026</div>
						<p class="education-description">
							Formación especializada en desarrollo de software, análisis de sistemas, 
							bases de datos, programación orientada a objetos y metodologías ágiles. 
							Desarrollo de proyectos prácticos aplicando tecnologías modernas.
						</p>
						<div class="education-tags">
							<span class="tag">Desarrollo Web</span>
							<span class="tag">Bases de Datos</span>
							<span class="tag">POO</span>
							<span class="tag">Metodologías Ágiles</span>
						</div>
					</div>
				</div>

				<!-- Universidad Santo Tomás -->
				<div class="timeline-item">
					<div class="timeline-marker">
						<div class="marker-dot"></div>
						<div class="marker-line"></div>
					</div>
					<div class="timeline-content">
						<h3 class="degree-title">Pregrado en Contaduría Pública</h3>
						<div class="institution">Universidad Santo Tomás Colombia</div>
						<div class="education-date">Febrero de 2017 - Abril de 2022</div>
						<p class="education-description">
							Formación integral en ciencias contables, financieras, tributarias y de auditoría. 
							Desarrollo de competencias en análisis financiero, normativa internacional y 
							gestión empresarial con énfasis en ética profesional.
						</p>
						<div class="education-tags">
							<span class="tag">Contabilidad</span>
							<span class="tag">Finanzas</span>
							<span class="tag">Auditoría</span>
							<span class="tag">Tributación</span>
						</div>
					</div>
				</div>

				<!-- Alafec -->
				<div class="timeline-item">
					<div class="timeline-marker">
						<div class="marker-dot"></div>
						<div class="marker-line"></div>
					</div>
					<div class="timeline-content">
						<h3 class="degree-title">Curso Internacional de Revisoría Fiscal</h3>
						<div class="institution">Asociación Latinoamericana de Facultades y Escuelas de Contaduría y Administración – Alafec</div>
						<div class="education-date">Noviembre de 2020 - Diciembre de 2020</div>
						<p class="education-description">
							Especialización en revisoría fiscal con perspectiva latinoamericana, 
							abordando marcos normativos internacionales, auditoría integral y 
							responsabilidades del revisor fiscal.
						</p>
						<div class="education-tags">
							<span class="tag">Revisoría Fiscal</span>
							<span class="tag">Auditoría</span>
							<span class="tag">Normativa Internacional</span>
						</div>
					</div>
				</div>

				<!-- Universidad Andina del Cusco -->
				<div class="timeline-item">
					<div class="timeline-marker">
						<div class="marker-dot"></div>
					</div>
					<div class="timeline-content">
						<h3 class="degree-title">Movilidad Estudiantil</h3>
						<div class="institution">Universidad Andina del Cusco</div>
						<div class="field">Contabilidad y Actividades Afines</div>
						<div class="education-date">Julio de 2019 - Diciembre de 2019</div>
						<p class="education-description">
							Experiencia internacional de intercambio académico en Cusco - Perú. 
							Inmersión en sistemas contables internacionales y 
							enriquecimiento cultural y profesional.
						</p>
						<div class="education-tags">
							<span class="tag">Intercambio</span>
							<span class="tag">Perú</span>
							<span class="tag">Contabilidad Internacional</span>
						</div>
					</div>
				</div>
			</div>

			<!-- Imagen de Programación -->
			<div class="education-visual">
				<div class="image-wrapper">
					<Image 
						src={estudiante} 
						alt="Programando"
						class="education-image"
						loading="lazy"
					/>
				</div>
				<div class="decoration-circle"></div>
			</div>
		</div>
	</div>
</section>

<style>
	.education-section {
		padding: var(--space-20) 0;
		position: relative;
		overflow: hidden;
		background: linear-gradient(180deg, var(--background) 0%, var(--secondary-100) 100%);
	}

	.container {
		max-width: 1280px;
		margin: 0 auto;
		padding: 0 clamp(1rem, 5vw, 3rem);
		width: 100%;
	}

	/* ===== HEADER ===== */
	.section-header {
		text-align: center;
		margin-bottom: var(--space-16);
	}

	.section-title {
		font-size: clamp(2rem, 5vw, 3.5rem);
		font-weight: 800;
		color: var(--text-primary);
		margin-bottom: 1rem;
	}

	.text-gradient {
		background: var(--gradient-2);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.section-subtitle {
		font-size: clamp(1rem, 2.5vw, 1.25rem);
		color: var(--text-secondary);
		max-width: 600px;
		margin: 0 auto;
	}

	/* ===== GRID ===== */
	.education-grid {
		display: grid;
		gap: var(--space-12);
		align-items: start;
	}

	/* ===== TIMELINE ===== */
	.education-timeline {
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}

	.timeline-item {
		display: grid;
		grid-template-columns: 40px 1fr;
		gap: 1.5rem;
		position: relative;
	}

	.timeline-marker {
		display: flex;
		flex-direction: column;
		align-items: center;
		position: relative;
	}

	.marker-dot {
		width: 16px;
		height: 16px;
		border-radius: 50%;
		background: var(--accent-teal);
		border: 3px solid var(--background);
		box-shadow: 0 0 0 3px rgba(20, 184, 166, 0.3);
		z-index: 2;
		flex-shrink: 0;
	}

	.marker-line {
		width: 2px;
		flex: 1;
		background: var(--secondary-300);
		margin-top: 0.5rem;
		min-height: 100%;
	}

	.timeline-item:last-child .marker-line {
		display: none;
	}

	/* ===== CONTENIDO ===== */
	.timeline-content {
		background: var(--white);
		padding: 1.5rem;
		border-radius: 1rem;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
		transition: all 0.3s ease;
		border: 1px solid transparent;
		position: relative;
	}

	.timeline-content:hover {
		transform: translateX(-8px);
		box-shadow: 0 8px 30px rgba(20, 184, 166, 0.15);
		border-color: rgba(20, 184, 166, 0.3);
	}

	.degree-title {
		font-size: 1.25rem;
		font-weight: 700;
		color: var(--text-primary);
		margin: 0 0 0.5rem 0;
		line-height: 1.4;
	}

	.institution {
		font-size: 1.125rem;
		font-weight: 600;
		color: var(--accent-teal);
		margin-bottom: 0.25rem;
	}

	.field {
		font-size: 0.875rem;
		color: var(--text-secondary);
		font-style: italic;
		margin-bottom: 0.25rem;
	}

	.education-date {
		font-size: 0.875rem;
		color: var(--text-secondary);
		margin-bottom: 0.75rem;
		font-weight: 500;
	}

	.education-description {
		color: var(--text-secondary);
		line-height: 1.7;
		margin: 1rem 0;
	}

	.education-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-top: 1rem;
	}

	.tag {
		font-size: 0.75rem;
		font-weight: 500;
		color: var(--accent-teal);
		background: linear-gradient(135deg, rgba(20, 184, 166, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
		padding: 0.375rem 0.75rem;
		border-radius: 0.5rem;
		border: 1px solid rgba(20, 184, 166, 0.3);
	}

	/* ===== IMAGEN ===== */
	.education-visual {
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
		margin-top: 2rem;
	}

	.image-wrapper {
		position: relative;
		width: 100%;
		max-width: 400px;
		z-index: 2;
		border-radius: 1rem;
		overflow: hidden;
		/* box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15); */
		transition: transform 0.3s ease;
	}

	.image-wrapper:hover {
		transform: translateY(-10px);
	}

	.education-image {
		width: 100%;
		height: auto;
		display: block;
	}

	.decoration-circle {
		position: absolute;
		width: 350px;
		height: 350px;
		border-radius: 50%;
		background: var(--gradient-2);
		filter: blur(80px);
		opacity: 0.2;
		z-index: 1;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
	}

	/* ===== RESPONSIVE ===== */
	@media (min-width: 1024px) {
		.education-grid {
			grid-template-columns: 1fr 400px;
			gap: var(--space-16);
			align-items: start;
		}

		.education-visual {
			position: sticky;
			top: 100px;
			margin-top: 0;
		}

		.timeline-content:hover {
			transform: translateX(-8px);
		}
	}

	@media (max-width: 640px) {
		.timeline-item {
			grid-template-columns: 30px 1fr;
			gap: 1rem;
		}

		.marker-dot {
			width: 12px;
			height: 12px;
		}

		.timeline-content {
			padding: 1.25rem;
		}

		.degree-title {
			font-size: 1.125rem;
		}
	}

	/* ===== ANIMACIONES ===== */
	@media (prefers-reduced-motion: no-preference) {
		.timeline-item {
			opacity: 0;
			animation: fadeInUp 0.6s ease-out forwards;
		}

		.timeline-item:nth-child(1) { animation-delay: 0.1s; }
		.timeline-item:nth-child(2) { animation-delay: 0.2s; }
		.timeline-item:nth-child(3) { animation-delay: 0.3s; }
		.timeline-item:nth-child(4) { animation-delay: 0.4s; }

		@keyframes fadeInUp {
			from {
				opacity: 0;
				transform: translateY(30px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	}
</style>
